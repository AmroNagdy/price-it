import styled from 'styled-components';
import { GREEN } from 'constants/AppColours';

export default styled.h1`
  align-self: center;
  text-align: center;
  justify-self: center;
  color: ${GREEN};
  font-size: 3rem;
`;