import styled from 'styled-components';
import { YELLOW } from 'constants/AppColours';

export default styled.h3`
  align-self: center;
  text-align: center;
  justify-self: center;
  color: ${YELLOW};
  font-size: 1.5rem;
`;