import React from 'react';

export default () => {

  return (
    <h2 style={{ margin: 'auto', textAlign: 'center', maxWidth: '60vw' }}>
      Select an asset class and model from the navigation bar above.
    </h2>
  );

};